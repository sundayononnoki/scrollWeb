name: Deploy GitHub Pages

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - name: Pages source
        run: echo "GitHub Pages is deployed from the main branch root in repository settings."
